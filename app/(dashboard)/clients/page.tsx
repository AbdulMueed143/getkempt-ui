import { ClientsPageClient } from "@/components/dashboard/clients/clients-page-client";

export const metadata = { title: "All Clients | GetKempt" };

export default function ClientsPage() {
  return <ClientsPageClient />;
}
