import { PackagePageClient } from "@/components/dashboard/packages/package-page-client";

export const metadata = { title: "Packages | GetKempt" };

export default function PackagesPage() {
  return <PackagePageClient />;
}
