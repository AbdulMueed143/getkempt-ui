import { ServicePageClient } from "@/components/dashboard/services/service-page-client";

export const metadata = { title: "Services | GetKempt" };

export default function ServicesPage() {
  return <ServicePageClient />;
}
